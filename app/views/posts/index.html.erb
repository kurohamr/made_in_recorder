<h1><%= current_user.name %>'s posts</h1>

  <table>
    <thead>
      <tr>
        <th>Thing_Name</th>
        <th>Place</th>
        <th>Description</th>
        <!-- <th>Image</th> -->
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @posts.where(user_id: current_user.id).each do |post| %>
        <tr>
          <td><%= post.thing %></td>
          <td><%= post.place %></td>
          <td><%= render_with_hashtags(post.description) %></td>
          <!-- <td>imatd</td> -->
          <td><%= link_to 'Show', post %></td>
          <td><%= link_to 'Edit', edit_post_path(post) %></td>
          <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
